[package]
name = "lucy_embedded_firmware_rp2040_support"
version = "0.1.0"
edition = "2024"

[dependencies]
rp2040-hal = { version = "0.11.0", features = ["rt", "critical-section-impl"] }
usb-device = "0.3.2"

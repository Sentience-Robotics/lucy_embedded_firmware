//! Board layout for `rp2040_bus_servo` (UART0 + device ids).
pub use lucy_embedded_firmware_core::board_layout::{
    BoardLayout, HardwareIdentity, Rp2040BusServoLayout,
};

pub type Layout = Rp2040BusServoLayout;
